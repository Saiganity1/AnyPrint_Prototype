build:
  publish: public
  command: bash build.sh
services:
  - type: static
    name: anyprint-frontend
    publish: public
    staticPublishOptions:
      redirect:
        - source: "/*"
          destination: "/index.html"