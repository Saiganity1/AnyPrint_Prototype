{
  "name": "anyprint-prototype-frontend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix react-vite && npm run build --prefix react-vite",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
